Brand template (HTML + CSS)
---------------------------

Files included:
 - index.html
 - brand-template.css

How to use locally:
 1.HTML
 2. Open index.html directly in your browser for a static preview:
    - double-click index.html or open it from your browser (file://...)
 3. Or serve it with a simple HTTP server (recommended for some browser features):
    - Python 3: run `python -m http.server 8000` inside the unzipped folder, then open http://localhost:8000

Customize:
 - Edit colors in :root variables at the top of brand-template.css
 - Adjust fonts by changing --font-sans
 - Replace copy and images in index.html

Enjoy! — Generated by ChatGPT
